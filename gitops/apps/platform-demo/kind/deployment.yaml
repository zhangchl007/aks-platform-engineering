apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-kind-demo
  namespace: group1-apps
  labels:
    app.kubernetes.io/name: argocd-kind-demo
    app.kubernetes.io/part-of: platform-demo
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-kind-demo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: argocd-kind-demo
    spec:
      containers:
        - name: podinfo
          image: ghcr.io/stefanprodan/podinfo:6.7.1
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9898
          env:
            - name: PODINFO_UI_MESSAGE
              value: "kind app reconciled by control-plane ArgoCD"
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
