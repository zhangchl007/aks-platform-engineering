apiVersion: v1
kind: Service
metadata:
  name: argocd-aks-demo
  namespace: group2-aks-apps
  labels:
    app.kubernetes.io/name: argocd-aks-demo
    app.kubernetes.io/part-of: platform-demo
    app.kubernetes.io/managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: argocd-aks-demo
  ports:
    - name: http
      port: 80
      targetPort: http
