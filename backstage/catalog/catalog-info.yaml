apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: platform-catalog
  description: Git-managed platform Catalog entrypoint.
spec:
  type: url
  targets:
    - ./platform-clusters.yaml
    - ../packages/examples/template/template.yaml
