# Generated by the Backstage "Request AKS Workload Cluster" Software Template.
# ArgoCD reads this file through gitops/clusters/capz/aks-appset.yaml and renders
# the CAPZ azure-managed-cluster Helm chart.
workloadClusterName: ${{ values.clusterName }}
resourceGroupName: ${{ values.resourceGroupName }}
location: ${{ values.location }}
kubernetesVersion: ${{ values.kubernetesVersion }}
agentSku: ${{ values.agentSku }}
agentCount: ${{ values.agentCount }}
systemPoolName: ${{ values.systemPoolName }}
fleetMemberName: ${{ values.fleetMemberName }}
fleetGroup: ${{ values.fleetGroup }}
sshPublicKey: '${{ values.sshPublicKey }}'
